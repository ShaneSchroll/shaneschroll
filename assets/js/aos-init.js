// initalize AOS animations
AOS.init({
    duration: 1200,
    offset: 100,
    once: true, // only animate once when scrolling down (no scroll up animation)
    mirror: false // do not animate out when scrolling past this element (up or down)
});