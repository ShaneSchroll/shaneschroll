{% if is_front_page %}
    <header class="home-header">
        <div class="home-header__inner-wrap">
            <div class="text-content"></div>
            <div class="image-content"></div>
        </div>
    </header>
{% else %}
    <header class="site-header"></header>
{% endif %}