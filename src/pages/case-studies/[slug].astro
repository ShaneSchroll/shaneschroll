---
import Layout from '../../layouts/Layout.astro';

export function getStaticPaths() {
    return [];
}

const { slug } = Astro.params;
---

<Layout title={slug ?? 'Case Study'} description="">
</Layout>
